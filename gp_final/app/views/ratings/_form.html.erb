<%= form_with(model: rating, local: true) do |form| %>
  <% if rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rating.errors.count, "error") %> prohibited this rating from being saved:</h2>

      <ul>
      <% rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :rating_user_id %>
  </div>

  <div class="field">
    <%= form.label :restaurant_id %>
    <%= form.text_field :restaurant_id, id: :rating_restaurant_id %>
  </div>

  <div class="field">
    <%= form.label :review %>
    <%= form.text_area :review, id: :rating_review %>
  </div>

  <div class="field">
    <%= form.label :restaurant_rating %>
    <%= form.number_field :restaurant_rating, id: :rating_restaurant_rating %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
