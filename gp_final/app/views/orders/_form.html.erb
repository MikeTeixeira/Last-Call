<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <input type="hidden" id="pickup_address" name="restaurant[pickup_address]" value="#{@restaurant.full_address}" />
    <input type="hidden" id="pickup_phone_number" name="restaurant[pickup_phone_number]" value="#{@restaurant.phone_number}" />
    <input type="hidden" id="pickup_business_name" name="restaurant[pickup_business_name]" value="#{@restaurant.name}" />

  <div class="field">
    <%= form.label :dropoff_name %>
    <%= form.text_field :dropoff_name, id: :order_dropoff_name %>
  </div>

  <div class="field">
    <%= form.label :dropoff_address %>
    <%= form.text_field :dropoff_address, id: :order_dropoff_address %>
  </div>

  <div class="field">
    <%= form.label :dropoff_phone_number %>
    <%= form.text_field :dropoff_phone_number, id: :order_dropoff_phone_number %>
  </div>

  <div class="field">
    <%= form.label :dropoff_phone_number %>
    <%= form.text_field :dropoff_phone_number, id: :order_dropoff_phone_number %>
  </div>

  <div class="field">
    <%= form.label :dropoff_business_name %>
    <%= form.text_field :dropoff_business_name, id: :order_dropoff_business_name %>
  </div>

  <div class="field">
    <%= form.label :dropoff_notes %>
    <%= form.textarea_field :dropoff_notes, id: :order_dropoff_notes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
